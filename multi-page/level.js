`use strict`;

// home btn
export const back = document.querySelector(`.back`);

// text
export const text = document.querySelector(`.text`);

export const level = document.querySelectorAll(`.level`)
